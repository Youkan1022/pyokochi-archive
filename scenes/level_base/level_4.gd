extends "res://scenes/level_base/level_base.gd"

## level3 固有設定
## 現在は固有設定なし。共通処理は level_base.gd が担当する。
## レベル固有の設定（BGM・風・パーティクル等）が必要になったら
## _level_setup() / _level_reset() をオーバーライドする。


func _level_setup() -> void:
	pass
